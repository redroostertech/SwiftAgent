import XCTest
import SwiftSyntaxMacrosTestSupport

/// Macro expansion tests for `@AgentTool` and `@Param`.
///
/// These tests verify that the macros produce the expected Swift source
/// code using swift-syntax's `assertMacroExpansion`. They run at compile
/// time — no simulator or device needed.
///
/// Phase 3 implementation — placeholder until the macro is implemented.
final class MacroExpansionTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true, "Macro tests will be added in Phase 3")
    }
}
